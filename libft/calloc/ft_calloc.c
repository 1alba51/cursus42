// Esta función reserva espacio de almacenamiento para una matriz de elementos num cada uno de ellos de longitud
// de tamaño bytes.
// Proporciona a todos los bits de cada elemento un valor inicial de 0
// Devuelve un puntero al espacio reservado.
// El prototipo de la función será:
// 			void	*ft_calloc(size_t nmemb, size_t size);

#include <stdio.h>

void	*ft_calloc(size_t nmemb, size_t size)
{
	
}

int	main(void)
{
	return(0);
}
